{
  "title": "Gmail Inbox Intelligence Input",
  "description": "Drops a Friday triage list of stalled Gmail client threads by SLA breach age. gmail.readonly only, no scraper, no bulk sender. Don't want to set OAuth up yourself? $99 Done-For-You triage report on Gumroad — I run the scan, you get the 1-page report in 7 days.",
  "type": "object",
  "schemaVersion": 1,
  "properties": {
    "feature": {
      "title": "Feature",
      "type": "string",
      "description": "Choose which workflow to run.",
      "editor": "select",
      "enum": [
        "thread_search",
        "reply_metrics",
        "summarizer",
        "unread_digest",
        "reengage_angle"
      ],
      "default": "thread_search"
    },
    "query": {
      "title": "Gmail Query",
      "type": "string",
      "description": "Gmail search query syntax, e.g. newer_than:7d in:inbox",
      "editor": "textfield"
    },
    "max_results": {
      "title": "Max Results",
      "type": "integer",
      "description": "Maximum number of threads to inspect in this run.",
      "default": 50,
      "minimum": 1,
      "maximum": 500
    },
    "oauth_token": {
      "title": "OAuth Config",
      "type": "object",
      "description": "Your own Gmail OAuth values. We do not persist your Gmail data or token secrets.",
      "editor": "json",
      "properties": {
        "refresh_token": {
          "title": "Refresh Token",
          "type": "string",
          "description": "OAuth refresh token for Gmail API",
          "editor": "textfield"
        },
        "client_id": {
          "title": "Client ID",
          "type": "string",
          "description": "Google OAuth client ID",
          "editor": "textfield"
        },
        "client_secret": {
          "title": "Client Secret",
          "type": "string",
          "description": "Google OAuth client secret",
          "editor": "textfield"
        }
      },
      "required": [
        "refresh_token",
        "client_id",
        "client_secret"
      ]
    },
    "openai_api_key": {
      "title": "LLM API Key (Optional)",
      "type": "string",
      "description": "Optional API key for `summarizer` (thread summaries) and `reengage_angle` (drafts 3 re-engage email options per cold thread grounded in recent news). Both features work without a key — summarizer skips, reengage_angle still returns news headlines.",
      "editor": "textfield",
      "isSecret": true
    },
    "summary_model": {
      "title": "Summary Model",
      "type": "string",
      "description": "Optional model name for summarizer feature.",
      "editor": "textfield",
      "default": "gpt-4o-mini"
    },
    "free_tier_user_id": {
      "title": "Free Tier User ID",
      "type": "string",
      "description": "Stable identifier for monthly free quota tracking in KeyValueStore.",
      "editor": "textfield"
    },
    "dry_run": {
      "title": "Dry Run",
      "type": "boolean",
      "description": "If true, validates input and auth flow without full feature execution.",
      "editor": "checkbox",
      "default": false
    }
  },
  "required": [
    "feature",
    "oauth_token"
  ]
}
